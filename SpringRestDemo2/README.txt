http://localhost:8080/file

GET returns a list of all images in database.
"/file/[UUID]" can be used to retrieve specific image.
All images are displayed using HTML.

POST allows sending an image through form-data within Postman.
